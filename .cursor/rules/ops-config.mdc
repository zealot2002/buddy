---
description: 运营策略配置 — 修改参数请走 api/config，勿散落硬编码
globs: api/config/**,api/data/walk-snippets.ts,src/hooks/useWalkGeofence.ts
alwaysApply: false
---

# 运营配置规范

## 单一数据源

所有可运营调整的参数收敛在 `api/config/`：

| 文件 | 内容 |
|------|------|
| `walk-config.ts` | 围栏半径、主动触发冷却/位移、定位参数、nearby 上限、模拟开关 |
| `speech-config.ts` | TTS 时长估算 |
| `tts-config.ts` | ElevenLabs model、voice 参数（不含 API Key） |
| `index.ts` | `APP_CONFIG` 聚合导出 |

边走边听语料在 **`seeds/*.sql`**（表结构见 `migrations/`），运行时读 D1。

## 修改流程

1. 只改 `api/config/*.ts` 中的常量
2. 改 walk 语料：编辑 `seeds/*.sql` → `npm run db:seed:local -- --force` → 生产 `npm run db:seed:remote`
3. 改 stories/companions：运行 `npm run sync:functions-data`

## 后续运营端

- `GET /api/config` — 全量配置（管理平台）
- `GET /api/config/walk` — 边走边听策略（客户端可热更新）
- 写入 API 待运营端实现；结构以 `AppConfig` / `WalkListenConfig` 类型为准
